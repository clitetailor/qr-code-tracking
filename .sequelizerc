const path = require('path')

module.exports = {
  config: path.resolve(__dirname, 'database.js'),
  'models-path': path.resolve(__dirname, 'server/database'),
  'seeders-path': path.resolve(
    __dirname,
    'server/database/seeders'
  ),
  'migrations-path': path.resolve(
    __dirname,
    'server/database/migrations'
  )
}
